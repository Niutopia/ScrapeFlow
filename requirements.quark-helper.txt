# Host-only dependency for scripts/scrapeflow_quark_helper.py.
# It supplies the maintained WebSocket client used for an existing local CDP
# connection.  It is intentionally not installed in the API Docker image.
aiohttp>=3.9,<4
