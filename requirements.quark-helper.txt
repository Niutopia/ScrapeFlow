# Runtime dependency for the typed Quark Helper Compose sidecar.
# It supplies the maintained HTTP/WebSocket client used for the explicitly
# configured, already-existing Quark CDP connection.
aiohttp>=3.9,<4
